import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorText: {
    color: 'red',
    marginBottom: 10,
    minHeight: 20,
  },
  input: {
    borderWidth: 1,
    padding: 10,
    marginBottom: 10,
  },
  inputError: {
    borderColor: 'red',
  },
  inputWrapper: {
    flex: 1,
    marginBottom: 15, // Ajusta según sea necesario
  },
});
